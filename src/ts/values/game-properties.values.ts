import IGameProperties from "@interfaces/game-properties.interface";

const VGameProperties: IGameProperties = {
  canvasId: "#game-canvas",
  canvasIdName: "game-canvas"
};

export default VGameProperties;
